Updated todo list

Completed:
- [x] **Spatial-index overflow & probe exhaustion handled** (Fast3DSpatialGrid::build fails fast with bool status on probe exhaustion or non-finite values)
- [x] **Fabricated normals replaced** (True covariance/Cardano closed-form estimation computed from gathered neighbors with NaN fallback when indeterminate, eliminating artificial (0,0,1))

Remaining items:
1. **Unchecked integer/index overflow**  
   Cell coordinates, voxel keys, `int` point indices, and size calculations can still overflow or truncate on large/extreme inputs.

2. **RANSAC confidence is incomplete**  
   It reports minimum inlier checks, but lacks robust residual-quality validation and a complete confidence/uncertainty contract.

3. **No frame/timestamp/calibration contract**  
   For robotics, the pipeline does not validate coordinate frames, timestamps, sensor calibration, or stale input.

4. **No supervisor-level fail-safe integration**  
   Statuses exist internally, but a consuming autonomy system still needs explicit degraded/failed perception handling.